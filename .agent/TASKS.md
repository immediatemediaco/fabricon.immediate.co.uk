# TASKS.md

> Generated by `rpi-plan`. Do not edit the section structure — `rpi-implement` reads this file programmatically.
> Sequential tasks are worked through one at a time. Parallel tasks are handed to `/fleet` after sequential work is complete.

---

## Sequential Tasks

> Tasks that must run in order. Dependencies, shared file writes, or ordering sensitivity make these unsafe to parallelise.

| ID | Status | Confidence | Task | File(s) | Difficulty | Category |
|---|---|---|---|---|---|---|
| T-01 | ⏳ Pending | High | Move track descriptions from Settings to Conference entity + add getDays() | Conference.php, Settings.php | Medium | refactor |
| T-02 | ⏳ Pending | High | Restructure Slot entity to per-track model with date field | Slot.php | Medium | feature |
| T-03 | ⏳ Pending | Medium | Doctrine migration with data transformation | migrations/VersionXXXX.php | Hard | infra |
| T-04 | ⏳ Pending | High | Update SlotRepository with structured query methods | SlotRepository.php | Easy | feature |
| T-05 | ⏳ Pending | High | Update admin controllers for new schema | ScheduleCrudController.php, ConferenceCrudController.php, SettingsCrudController.php | Medium | feature |
| T-06 | ⏳ Pending | Medium | Update ConferenceController — schedule data grouped by day with time-grid positioning | ConferenceController.php | Hard | feature |
| T-07 | ⏳ Pending | High | Update DataFixtures for new schema | AppFixtures.php | Easy | infra |

---

### T-01 — Move track descriptions from Settings to Conference entity + add getDays() helper

**Context:** Track descriptions are currently global in the `Settings` entity (`track1Description`, `track2Description`). The research Q&A confirms these should move to `Conference` so each conference can have its own track descriptions. Additionally, `Conference` needs a `getDays()` helper to enumerate each day in the startDate→endDate range, which is needed downstream for multi-day admin grouping and template day tabs.

**Depends on:** None

**Update Copilot Instructions:** Maybe

**What Needs Doing:**
1. In `src/Entity/Conference.php`: add `track1Description` (text, nullable) and `track2Description` (text, nullable) properties with ORM column attributes, plus getters and setters
2. In `src/Entity/Conference.php`: add `getDays(): array` method that returns an array of `\DateTimeInterface` objects for each day from `startDate` to `endDate` (inclusive). For single-day conferences where `endDate` is null, return `[$this->startDate]`
3. In `src/Entity/Settings.php`: remove `track1Description` and `track2Description` properties, their ORM column attributes, getters, and setters — these fields will be dropped in the migration (T-03)
4. In `src/Controller/Admin/ConferenceCrudController.php`: add `TextareaField::new('track1Description', 'Track 1 Description')` and `TextareaField::new('track2Description', 'Track 2 Description')` to `configureFields()`
5. In `src/Controller/Admin/SettingsCrudController.php`: remove the two `TextareaField` lines for `track1Description` and `track2Description`

**Difficulty:** Medium

**Files:**
- `src/Entity/Conference.php` — add track1Description, track2Description columns + getDays() method
- `src/Entity/Settings.php` — remove track1Description, track2Description columns
- `src/Controller/Admin/ConferenceCrudController.php` — add track description fields to form
- `src/Controller/Admin/SettingsCrudController.php` — remove track description fields from form

**How to Test:**
- `php bin/console lint:container` — verify no service container errors
- Visually inspect entity code: Conference should have 2 new text properties with ORM attributes; Settings should no longer have them
- `getDays()` with startDate=2026-03-20 and endDate=2026-03-22 should return array of 3 DateTimeInterface objects
- `getDays()` with endDate=null should return array of 1 element (startDate)

**Unexpected Outcomes:**
- `getDays()` must handle the case where endDate equals startDate (returns single-element array, not empty)
- The ORM column removal from Settings will cause Doctrine to flag the schema as out of sync until T-03 migration is run

---

### T-02 — Restructure Slot entity to per-track model with date field

**Context:** The current `Slot` entity pairs both tracks into one row via `track1` and `track2` ManyToOne references to `Talk`. This prevents independent time slots per track. The research recommends Option A: replace `track1`/`track2` with a single `talk` reference plus a `track` integer column, and add a `date` column for multi-day support. Breaks are represented by `track = null` (spanning both tracks).

**Depends on:** None

**Update Copilot Instructions:** Yes

**What Needs Doing:**
1. Remove the `track1` and `track2` ManyToOne properties (with their ORM attributes, getters, and setters) from `src/Entity/Slot.php`
2. Add `talk` property: `#[ORM\ManyToOne(targetEntity: Talk::class)]` — nullable, with getter/setter
3. Add `track` property: `#[ORM\Column(type: 'smallint', nullable: true)]` — values: 1, 2, or null (null = break spanning both tracks). Add getter/setter
4. Add `date` property: `#[ORM\Column(type: 'date')]` — not null, represents which day of the conference. Add getter/setter
5. Add `isBreak(): bool` convenience method that returns `true` when `breakDetails` is not null/empty
6. Keep existing `breakDetails`, `getBreakStyle()`, `conference`, `startTime`, `endTime` properties unchanged

**Difficulty:** Medium

**Files:**
- `src/Entity/Slot.php` — restructure from paired-track to per-track model

**How to Test:**
- `php bin/console lint:container` — verify no service container errors
- Slot entity should have: id, date, startTime, endTime, talk, track, breakDetails, conference
- Slot entity should NOT have: track1, track2
- `isBreak()` returns true when breakDetails is set, false otherwise

**Unexpected Outcomes:**
- Any code that references `$slot->getTrack1()` or `$slot->getTrack2()` will break — these are addressed in T-05, T-06, T-07/T-08
- The `date` column is NOT NULL, so a default must be handled in the migration (T-03)

---

### T-03 — Doctrine migration with data transformation

**Context:** T-01 and T-02 changed three entities. This migration must transform the production database schema AND preserve all existing data. The migration must: (a) move track descriptions from settings to conference, (b) restructure slot table from paired-track to per-track rows, and (c) drop obsolete columns. The research Q&A confirms production data needs careful migration.

**Depends on:** T-01, T-02

**Update Copilot Instructions:** No

**What Needs Doing:**
1. Generate the migration skeleton: `php bin/console doctrine:migrations:diff` (this creates the schema changes automatically from entity diffs)
2. In the generated migration `up()` method, add data transformation SQL BEFORE the DROP COLUMN statements. Insert the following data operations in this order:
3. Copy track descriptions: `UPDATE conference SET track1_description = (SELECT track1_description FROM settings WHERE id = 1), track2_description = (SELECT track2_description FROM settings WHERE id = 1)`
4. For slot data: first add the new columns (talk_id, track, date) as nullable, then populate them from existing data. For each existing slot row: if track1_id is not null, INSERT a new row with talk_id=track1_id, track=1, date=(SELECT start_date FROM conference WHERE id=slot.conference_id). If track2_id is not null, INSERT another row with talk_id=track2_id, track=2, same date. If both are null (break), UPDATE the existing row setting track=null and date from conference.
5. After data is transformed, DROP the old columns: track1_id, track2_id from slot; track1_description, track2_description from settings
6. Add ALTER TABLE to set `date` column as NOT NULL after data is populated
7. Write the `down()` method to reverse all changes (restore track1_id, track2_id columns, merge per-track rows back, etc.)

**Difficulty:** Hard

**Files:**
- `migrations/VersionXXXXXXXXXXXXXX.php` — new migration file (auto-generated filename)

**How to Test:**
- `php bin/console doctrine:migrations:migrate --dry-run` — verify SQL is valid
- `php bin/console doctrine:migrations:migrate` — run against dev database
- `php bin/console doctrine:schema:validate` — verify schema matches entities
- Verify: each old slot with track1 populated should produce a row with track=1; same for track2; breaks should remain as single rows with track=null
- Verify: conference rows should now have track descriptions copied from settings

**Unexpected Outcomes:**
- If any slot had both track1 and track2 set, the original row needs to be split into TWO new rows. The migration must handle auto-increment IDs correctly — INSERT new rows rather than UPDATE in place for the split case
- The self-referencing data transformation is complex: consider using a temporary table or careful INSERT/UPDATE sequencing to avoid conflicts
- `down()` migration is complex due to the row-merging requirement; test rollback in dev

---

### T-04 — Update SlotRepository with structured query methods

**Context:** The current `SlotRepository` has no custom methods — it relies on Doctrine's magic `findByConference()`. The new per-track schema needs queries that return slots ordered by date, then startTime, then track — and optionally filtered by date for single-day views in the admin.

**Depends on:** T-02

**Update Copilot Instructions:** No

**What Needs Doing:**
1. Add `findScheduleByConference(Conference $conference): array` — returns all slots for a conference, ordered by `date ASC, startTime ASC, track ASC NULLS FIRST` (breaks before track slots at the same time)
2. Add `findByConferenceGroupedByDay(Conference $conference): array` — calls `findScheduleByConference()` then groups results into an associative array keyed by date string (`Y-m-d`), e.g. `['2026-03-20' => [Slot, Slot, ...], '2026-03-21' => [...]]`
3. Remove the `@method` phpdoc for `findByConference` (it will still work via Doctrine magic but is no longer the primary query path)

**Difficulty:** Easy

**Files:**
- `src/Repository/SlotRepository.php` — add findScheduleByConference() and findByConferenceGroupedByDay()

**How to Test:**
- `php bin/console lint:container` — verify no service container errors
- Write a quick functional test or manually verify: given a conference with multi-day slots, `findByConferenceGroupedByDay()` returns slots grouped by date with correct ordering within each day

**Unexpected Outcomes:**
- `NULLS FIRST` is PostgreSQL-specific syntax; verify Doctrine DQL supports this or use `COALESCE(s.track, 0)` for ordering instead

---

### T-05 — Update admin controllers for new Slot schema

**Context:** The `ScheduleCrudController` currently shows `track1` and `track2` as separate association fields because the old schema paired them in one row. With the new per-track model, each slot row represents a single track entry (or a break). The admin form needs fields for date, track (choice: 1/2), talk (single association), plus the existing time and break fields. ConferenceCrudController needs track description fields. SettingsCrudController already had its fields removed in T-01.

**Depends on:** T-01, T-02, T-04

**Update Copilot Instructions:** No

**What Needs Doing:**
1. In `ScheduleCrudController::configureCrud()`: update `setDefaultSort` to `['date' => 'ASC', 'startTime' => 'ASC', 'track' => 'ASC']`
2. In `ScheduleCrudController::configureFields()`: replace the "Talk Details" panel's `track1`/`track2` AssociationFields with a single `AssociationField::new('talk', 'Talk')` and a `ChoiceField::new('track', 'Track')->setChoices(['Track 1' => 1, 'Track 2' => 2])->allowMultipleChoices(false)->renderExpanded(false)`
3. In `ScheduleCrudController::configureFields()`: add `DateField::new('date')` to the "Slot Details" panel (before startTime)
4. Update `ScheduleCrudController::createIndexQueryBuilder()`: add ordering by `entity.date ASC, entity.startTime ASC, entity.track ASC` alongside the existing Settings join filter
5. Ensure the `ConferenceCrudController` changes from T-01 are in place (track description fields added)
6. Verify `SettingsCrudController` changes from T-01 are in place (track description fields removed)

**Difficulty:** Medium

**Files:**
- `src/Controller/Admin/ScheduleCrudController.php` — new fields, sort order, index query
- `src/Controller/Admin/ConferenceCrudController.php` — verified from T-01
- `src/Controller/Admin/SettingsCrudController.php` — verified from T-01

**How to Test:**
- Start the dev server and navigate to `/admin` → Schedule
- Verify the slot list shows date, startTime, endTime, talk, track, breakDetails columns
- Verify list is sorted by date then time then track
- Create a new slot: form should show date picker, start/end time, track dropdown (1/2), talk association, break details
- Edit an existing slot: all fields should populate correctly

**Unexpected Outcomes:**
- EasyAdmin `ChoiceField` may need `->renderAsNativeWidget(true)` or similar for proper display
- The track field should be nullable for breaks — may need to add an empty/null option to the ChoiceField choices: `['' => null, 'Track 1' => 1, 'Track 2' => 2]`
- The date field should default to the conference's startDate when creating a new slot; this may require a custom `createEntity()` override

---

### T-06 — Update ConferenceController — group schedule by day with time-grid positioning

**Context:** The controller currently passes a flat list of slots to templates. For multi-day conferences with independent track timing, the controller must prepare structured data: slots grouped by day, with time-grid positioning metadata so templates can render tracks with time-synchronised vertical alignment. The research identifies this as the most complex part of the feature.

**Depends on:** T-01, T-04

**Update Copilot Instructions:** Maybe

**What Needs Doing:**
1. In `ConferenceController::show()`: replace `$this->slots->findByConference(...)` with `$this->slots->findByConferenceGroupedByDay($conference)`
2. Create a private method `prepareScheduleData(array $slotsByDay, Conference $conference): array` that transforms each day's slots into a structured array
3. For each day: separate slots into track1 slots (track=1), track2 slots (track=2), and breaks (track=null). Calculate the day's time range (earliest startTime to latest endTime across all tracks)
4. For each slot: compute `gridRowStart` and `gridRowEnd` values based on 5-minute time increments. Formula: `gridRowStart = (slotStartMinutes - dayStartMinutes) / 5 + 1`, `gridRowEnd = (slotEndMinutes - dayStartMinutes) / 5 + 1`. Also compute `totalRows = (dayEndMinutes - dayStartMinutes) / 5`
5. Pass the structured data to the template: `$scheduleData = ['days' => [['date' => DateTimeInterface, 'dateFormatted' => string, 'track1Slots' => [...], 'track2Slots' => [...], 'breaks' => [...], 'totalRows' => int]]]`
6. Update the template render calls to pass `scheduleData` and `conference` (which now has track descriptions) instead of `schedule` and `settings` for track descriptions. Keep passing `settings` for other fields (conferenceDetails, introduction, currentConference).

**Difficulty:** Hard

**Files:**
- `src/Controller/ConferenceController.php` — schedule data preparation with time-grid calculation

**How to Test:**
- Add a `dump()` call in the controller and verify the structured data shape
- Given track 1 slot 10:00–11:00 and track 2 slot 10:30–11:30: track 1 should get gridRowStart=1, gridRowEnd=13 (60 min / 5 = 12 rows); track 2 should get gridRowStart=7, gridRowEnd=19 (offset 30min = 6 rows). totalRows = 90min / 5 = 18
- Single-day conference should produce one day entry
- Multi-day conference should produce one entry per day

**Unexpected Outcomes:**
- Edge case: a day with only breaks and no talks — totalRows calculation must still work
- Edge case: empty days (conference spans 3 days but only days 1 and 3 have slots) — decide whether to show empty day tabs or skip them (recommend: only show days that have slots)
- The 5-minute granularity may need adjustment if slots don't align to 5-minute boundaries — consider using 1-minute granularity for maximum flexibility, but this increases the grid row count

---

### T-07 — Update DataFixtures for new schema

**Context:** `AppFixtures` currently creates a Conference and Settings with track descriptions on Settings. After T-01 and T-02, track descriptions live on Conference and Slot uses per-track rows. The fixtures need updating to match the new schema so `doctrine:fixtures:load` works correctly.

**Depends on:** T-01, T-02

**Update Copilot Instructions:** No

**What Needs Doing:**
1. Move `setTrack1Description()` and `setTrack2Description()` calls from `$settings` to `$conference`
2. Remove the `track1Description`/`track2Description` lines from the Settings creation block
3. Optionally add sample Slot fixtures using the new per-track model: create 2–3 slots per track plus one break to demonstrate the new schema
4. Set `date` on each sample Slot to the conference's startDate

**Difficulty:** Easy

**Files:**
- `src/DataFixtures/AppFixtures.php` — update for new entity structure

**How to Test:**
- `php bin/console doctrine:fixtures:load --no-interaction` — should complete without errors
- Verify database: conference row should have track descriptions; settings row should not; slot rows should have date, track, talk_id columns

**Unexpected Outcomes:**
- If fixtures are loaded against an existing database without running migrations first, it will fail — ensure migration (T-03) has been run

---

## Parallel Tasks

> Tasks that are fully independent — no dependencies on other tasks, no shared file writes, safe to run simultaneously with `/fleet`.

| ID | Status | Confidence | Task | File(s) | Difficulty | Category |
|---|---|---|---|---|---|---|
| T-08 | ⏳ Pending | Medium | Rewrite 2026 template + SCSS — multi-day tabs, time-grid, remove hard-coded text | 2026/index.html.twig, 2026/_schedule.scss | Hard | feature |
| T-09 | ⏳ Pending | Medium | Rewrite 2023 template + SCSS — multi-day tabs, time-grid, remove hard-coded text | june-2023/index.html.twig, 2023/_schedule.scss | Hard | feature |

---

### T-08 — Rewrite 2026 template + SCSS — multi-day tabs, time-grid, remove hard-coded text

**Context:** The 2026 theme template currently iterates a flat slot list and renders track 1 / track 2 side-by-side assuming shared timing. It must be rewritten to: (a) support multi-day conferences with tab navigation, (b) render each track's slots independently with time-proportional vertical positioning via CSS Grid, (c) display breaks spanning both columns, (d) remove the hard-coded "Track 1 - " / "Track 2 - " prefix text, and (e) use `conference.track1Description` instead of `settings.track1Description`. This task is fully independent of T-09 (2023 template) — they touch different files.

After all sequential tasks complete, the controller passes `scheduleData` (structured by day with `gridRowStart`/`gridRowEnd` per slot and `totalRows` per day) and `conference` (with `track1Description`/`track2Description`). The template must consume this structure.

**Depends on:** None (runs after all sequential tasks)

**Update Copilot Instructions:** No

**What Needs Doing:**
1. Add day tab navigation above the schedule section: render a `<div class="schedule__day-tabs">` with one button per day in `scheduleData.days`, showing the formatted date. Hide the tabs entirely for single-day conferences. Use a Stimulus controller or simple JS to toggle visibility of day sections, or use anchor links to scroll to each day section.
2. Replace the current `{% for slot in schedule %}` loop with a `{% for day in scheduleData.days %}` loop. Each day gets a `<div class="schedule__day" id="day-{{ day.date|date('Y-m-d') }}">` wrapper with a day heading (e.g., "Day 1 — Thursday 20 March 2026").
3. Inside each day, render a CSS Grid container: `<div class="schedule__day-grid" style="grid-template-rows: repeat({{ day.totalRows }}, minmax(4px, 1fr))">`. Iterate `day.track1Slots` rendering each as a `<div class="session session__track--1" style="grid-row: {{ slot.gridRowStart }} / {{ slot.gridRowEnd }}; grid-column: 2;">`. Same for `day.track2Slots` in grid-column 3. Same for `day.breaks` spanning `grid-column: 2 / -2`.
4. Update track heading: change `<h3 class="track__title">Track 1 - {{ settings.track1Description }}</h3>` to `<h3 class="track__title">{{ conference.track1Description }}</h3>` — removing the hard-coded "Track 1 - " prefix. Same for Track 2.
5. Update `session__location` divs: change `{{ settings.track1Description }}` to `{{ conference.track1Description }}` and same for track 2.
6. Update `assets/styles/2026/components/_schedule.scss`: add `.schedule__day-tabs` styling (horizontal tab bar, active state), `.schedule__day-grid` (2-column grid with gutter columns: `grid-template-columns: var(--gutter) 1fr 1fr var(--gutter)`), and `.schedule__day-heading` styling. Ensure breaks still span both track columns.
7. Add responsive SCSS for mobile (≤768px): day tabs as horizontal scrollable buttons, schedule grid collapses to single column (`grid-template-columns: var(--padding) 1fr var(--padding)`), sessions stack vertically with track labels shown, day grid auto-rows instead of time-proportional grid.

**Difficulty:** Hard

**Files:**
- `templates/conference/2026/index.html.twig` — template rewrite for multi-day + time-grid
- `assets/styles/2026/components/_schedule.scss` — SCSS for time-grid layout + day tabs

**How to Test:**
- Build frontend: `yarn dev`
- Navigate to a single-day conference page — should display identically to current layout (no day tabs, slots aligned correctly)
- Navigate to a multi-day conference — day tabs should appear, clicking each tab should show that day's schedule
- With track 1 slot 10:00–11:00 and track 2 slot 10:30–11:30: track 2 card should visually start halfway through track 1's card and extend below it
- Breaks should span the full width of both track columns
- On mobile (≤768px): day tabs should be visible, tracks should stack vertically, location labels should appear
- Verify no "Track 1 - " or "Track 2 - " prefix text appears — only the description

**Unexpected Outcomes:**
- CSS Grid `minmax(4px, 1fr)` row sizing may produce very small rows for long days (e.g., 8:00–18:00 = 120 rows × 4px min = 480px). Consider a minimum height per row or a `minmax(6px, 1fr)` for better proportions
- If a track has no slots for a day, the column should still render (empty) to maintain the 2-column layout
- Day tab visibility toggle could use a simple Stimulus controller (`schedule-controller.js`) or vanilla JS with `data-*` attributes — keep it simple, no heavy framework

---

### T-09 — Rewrite 2023 template + SCSS — multi-day tabs, time-grid, remove hard-coded text

**Context:** The 2023 theme template (`june-2023/index.html.twig`) is nearly identical to the 2026 template and needs the same changes. This task is fully independent of T-08 — they touch completely different files (`june-2023/*` vs `2026/*`). The 2023 template extends `base-2023.html.twig` and uses `assets/styles/2023/` for its SCSS.

After all sequential tasks complete, the controller passes `scheduleData` and `conference` with the same structure described in T-08.

**Depends on:** None (runs after all sequential tasks)

**Update Copilot Instructions:** No

**What Needs Doing:**
1. Add day tab navigation above the schedule section — same pattern as T-08: `<div class="schedule__day-tabs">` with buttons per day, hidden for single-day conferences
2. Replace the `{% for slot in schedule %}` loop with `{% for day in scheduleData.days %}` loop, each day in a `.schedule__day` wrapper with a day heading
3. Render the time-synced CSS Grid per day — same structure: `schedule__day-grid` with grid-template-rows based on `day.totalRows`, track 1 slots in column 2, track 2 in column 3, breaks spanning both
4. Remove hard-coded text: change `Track 1 - {{ settings.track1Description }}` to `{{ conference.track1Description }}` in the track heading, and `{{ settings.track2Description }}` to `{{ conference.track2Description }}`. Update `session__location` divs similarly
5. Update `assets/styles/2023/components/_schedule.scss` with the same grid + tab styles as T-08 (the 2023 and 2026 SCSS files are currently identical, so the same changes apply)
6. Add responsive SCSS for mobile — same approach as T-08

**Difficulty:** Hard

**Files:**
- `templates/conference/june-2023/index.html.twig` — template rewrite for multi-day + time-grid
- `assets/styles/2023/components/_schedule.scss` — SCSS for time-grid layout + day tabs

**How to Test:**
- Build frontend: `yarn dev`
- Same test scenarios as T-08 but navigate to a conference using the `june-2023` theme
- Verify no "Track 1 - " or "Track 2 - " prefix text
- Verify day tabs, time-synced grid, break spanning, and responsive behaviour

**Unexpected Outcomes:**
- The 2023 template uses different images (bar-area.jpg etc.) and extends `base-2023.html.twig` — ensure these are preserved, only the schedule section changes
- The 2023 SCSS may diverge from 2026 in future — keep as separate files even though content is currently identical

---

## Execution Confidence

<!-- Written by rpi-implement after sequential tasks complete. Do not edit manually. -->
